{
  "name": "lab-jsonserver",
  "version": "1.0.0",
  "description": "Aplicação Web com backend baseado no JSON Server",
  "main": "index.js",
  "scripts": {
    "start": "node ./scripts/generate-env.js && npx json-server --watch ./public/db/db.json --static ./public --port 3000"
  },
  "keywords": [],
  "author": "Rommel Vieira Carneiro",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "json-server": "^0.17.4"
  }
}
